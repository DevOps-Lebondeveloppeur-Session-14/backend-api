name: Back End Deployment

on:
    push:
        branches:
            - main

jobs:
    # Unit Test
    Unit_Test:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v5

            - name: Install Dependencies
              run: npm i

            - name: Run Unit Test
              run: npm run test

    # Build And Push

    # Deployment